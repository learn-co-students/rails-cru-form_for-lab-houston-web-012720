<%= form_for @artist do |f| %> 
    <%= f.label :name %><br>
    <%= f.text_field :name %> 
    <br>
    <%= f.label :bio %><br>
    <%= f.text_area :bio %>
    <br>
    <%= f.submit %>
<% end %> 
<br>
<%= button_to "Artists page", artists_path, method: :get %> 
